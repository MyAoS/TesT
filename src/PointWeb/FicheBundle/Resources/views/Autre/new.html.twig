{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Autre creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('autre') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
